class ApiConfig {
  // Use "http://localhost:8000" when running Flutter on web or Windows desktop.
  // Switch to "http://10.0.2.2:8000" when running on an Android emulator.
  // Switch to your machine's local IP (e.g. "http://192.168.x.x:8000") for a real device.
  static const String baseUrl = "http://localhost:8000";
}